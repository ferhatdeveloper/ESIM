# Voyage eSIM (Flutter)

MVVM + Riverpod + GoRouter. HTTP yalnızca PostgREST’e gider.

```bash
flutter pub get
flutter test
flutter run --dart-define=POSTGREST_URL=http://127.0.0.1:3000
```

Android emülatöründe taban URL `http://10.0.2.2:3000` olmalıdır.
