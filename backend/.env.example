POSTGRES_DB=esim
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace-with-strong-password
POSTGRES_PORT=5432

PGRST_DB_USER=authenticator
PGRST_DB_PASSWORD=authenticator_dev_only
POSTGREST_PORT=3000

# Must match app_private.config jwt_secret (seeded in 11_seed.sql).
JWT_SECRET=dev-only-change-me-use-32-plus-chars!!
JWT_AUDIENCE=esim-app
